import type { StackScreenProps } from '@react-navigation/stack';
import { CompositeScreenProps } from '@react-navigation/native';
import { MainStackParamList, Screens } from '../../constants/navigator.constants';

export { Screens };
export type { MainStackParamList, StackScreenProps, CompositeScreenProps };
